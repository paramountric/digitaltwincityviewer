{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "projects/dtcc-file-viewer"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "publish-packages": "turbo run build && changeset version && changeset publish"
  },
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "dependencies": {
    "@changesets/cli": "^2.25.2"
  }
}
