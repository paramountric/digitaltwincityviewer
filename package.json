{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap",
    "tsc": "lerna run tsc --stream --no-private",
    "watch": "lerna run watch --stream --no-private",
    "test": "lerna run test --no-private",
    "publish": "lerna publish from-package --no-private"
  },
  "devDependencies": {
    "lerna": "^5.4.0"
  }
}
