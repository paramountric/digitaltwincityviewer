{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "projects/*",
    "website/"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dtcv-next-template-dev": "turbo run dev --scope 'dtcv-next-template' --include-dependencies",
    "dtcc-cfd-vertical-dev": "turbo run dev --filter dtcc-cfd-vertical...",
    "dte-digital-twin-energy-dev": "turbo run dev --filter dte-digital-twin-energy...",
    "trecim-citygml-viewer-dev": "turbo run dev --filter trecim-citygml-viewer...",
    "dtcv-website-dev": "turbo run dev --filter dtcv-website...",
    "publish-packages": "turbo run build && changeset version && changeset publish"
  },
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "dependencies": {
    "@changesets/cli": "^2.25.2"
  }
}
